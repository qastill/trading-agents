# Salin file ini ke .env lalu isi API key DeepSeek kamu.
# Tanpa prefix VITE_ → kunci ini hanya dipakai di sisi server (dev middleware /
# serverless function), tidak pernah dikirim ke browser.
# Ambil key di: https://platform.deepseek.com/api_keys
DEEPSEEK_API_KEY=sk-...
